// railway.json
{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && playwright install --with-deps chromium"
  },
  "deploy": {
    "startCommand": "sh -c \"uvicorn main:app --host 0.0.0.0 --port ${PORT:-8080} --workers 1\""
  }
}
